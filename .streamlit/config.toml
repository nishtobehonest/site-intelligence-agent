[theme]
base = "light"
primaryColor = "#0086A8"
backgroundColor = "#FFFFFF"
secondaryBackgroundColor = "#F0F2F6"
textColor = "#1A1A2E"
font = "sans serif"

[browser]
gatherUsageStats = false

[server]
# Raise the websocket max message size for large response payloads
maxMessageSize = 50
